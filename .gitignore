# build artifacts
dist/
# test
coverage.out
# sightjack runtime state on target repository
.siren/
# environment-specific local files (must not contain full paths)
*.local.*
# claude local settings
.claude/*.local.*
# git worktrees
.worktrees/
# planning docs (ephemeral, per-session)
docs/plans/
# golang
.gocache/
