version: 2

project_name: sightjack

before:
  hooks:
    - go mod tidy
    - go test ./... -count=1 -short

builds:
  - main: ./cmd/sightjack/
    binary: sightjack
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/hironow/sightjack/internal/cmd.version={{.Version}}
      - -X github.com/hironow/sightjack/internal/cmd.commit={{.ShortCommit}}
      - -X github.com/hironow/sightjack/internal/cmd.date={{.Date}}

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt
  algorithm: sha256

signs:
  - cmd: cosign
    args:
      - sign-blob
      - --yes
      - --output-signature=${signature}
      - ${artifact}
    artifacts: checksum

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - repository:
      owner: hironow
      name: homebrew-tap
    homepage: "https://github.com/hironow/sightjack"
    description: "SIREN-inspired issue architecture tool for Linear"
    license: "Apache-2.0"
    install: |
      bin.install "sightjack"
    test: |
      system "#{bin}/sightjack", "version"
