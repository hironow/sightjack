Boundaries section is no longer reflective of SSO requirements.

## Contract Amendments

- Contract: `auth-session-expiry` (revision 2)
- Amend Boundaries: Allow short-lived OAuth refresh tokens. (rationale: SSO requires refresh.)
- Amend Evidence: Add nfr.p95_latency_ms: <= 250 entry.
